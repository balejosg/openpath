#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# No verification needed here - already done in pre-commit
# This hook is kept for future use if needed
echo "Pre-push: verification already done in pre-commit"
